export const enum MoebooruRating {
  Safe = "s",
  Sensitive = "q",
  Explicit = "e",
}

export const enum GelbooruRating {
  Safe = "general",
  Questionable = "questionable",
  Sensitive = "sensitive",
  Explicit = "explicit",
}
